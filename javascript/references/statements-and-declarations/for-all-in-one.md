# for all in one


```js
const log = console.log;
const obj = {
  a: 1,
  b: 2,
};
for(const in obj) {
 log(`for in object i =`, i)
}
```



```js
const log = console.log;
const obj = {
  a: 1,
  b: 2,
};
for(const in obj) {
 log(`for in object i =`, i)
}
```



```js
const log = console.log;
const obj = {
  a: 1,
  b: 2,
};
for(const in obj) {
 log(`for in object i =`, i)
}
```
